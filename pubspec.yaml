name: klik_jasa
description: "KlikJasa - Marketplace jasa modern yang mempertemukan pengguna dengan penyedia jasa terverifikasi di seluruh Indonesia."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.5.0

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter


  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8

  # State Management
  flutter_bloc: ^8.1.3
  equatable: ^2.0.5
  bloc: ^8.1.4 # Added to fix depend_on_referenced_packages

  # Firebase - Hanya yang diperlukan untuk Google Sign In
  firebase_core: ^3.8.0
  firebase_auth: ^5.3.3
  
  # Notifications
  flutter_local_notifications: ^18.0.1

  # Supabase
  supabase_flutter: ^2.8.3

  # HTTP Client
  dio: ^5.7.0 # Versi terbaru saat ini, bisa disesuaikan

  # Navigation
  go_router: ^16.1.0
  intl: ^0.20.2

  # UI & Styling
  google_fonts: ^6.3.0
  flutter_svg: ^2.0.14
  cached_network_image: ^3.4.1
  shimmer: ^3.0.0
  image_picker: ^1.1.2
  image_cropper: ^8.0.2
  flutter_image_compress: ^2.3.0
  carousel_slider: ^5.1.1
  extended_image: ^8.3.1
  photo_view: ^0.15.0
  google_maps_flutter: ^2.10.0 # Untuk Google Maps
  geolocator: ^14.0.2 # Opsional, untuk mendapatkan lokasi saat ini.
  dartz: ^0.10.1 # Untuk Either type dan functional programming
  logger: ^2.3.0 # Untuk logging yang lebih baik
  path: ^1.9.0 # Untuk manipulasi path file
  url_launcher: ^6.3.1
  get_it: ^8.2.0 # Service Locator
  connectivity_plus: ^6.1.0 # Untuk cek koneksi jaringan
  shared_preferences: ^2.3.3 # Untuk menyimpan data sederhana (key-value)
  file_picker: ^10.2.0
  flutter_slidable: ^3.1.1 # Untuk fitur slide-to-action pada item notifikasi
  share_plus: ^11.1.0 # Untuk berbagi konten ke aplikasi lain
  package_info_plus: ^8.3.1 # Untuk mendapatkan info paket aplikasi (versi, dll)
  fl_chart: ^0.69.0
  pdf: ^3.11.3
  printing: ^5.14.2
  excel: ^4.0.6
  path_provider: ^2.1.5
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  flutter_rating_bar: ^4.0.1
  multi_select_flutter: ^4.1.3
  local_auth: ^2.3.0 # Untuk autentikasi biometrik (fingerprint, face ID, dll)
  google_sign_in: ^7.1.1 # Untuk login dengan Google
  sign_in_with_apple: ^7.0.1 # Untuk login dengan Apple
  crypto: ^3.0.3 # Untuk generate nonce untuk Apple Sign In
  uuid: ^4.3.3 # Untuk generate UUID
  flutter_staggered_grid_view: ^0.7.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^5.0.0 # Pastikan versi ini sesuai dengan yang Anda gunakan, atau ^3.0.0 atau ^4.0.0
  mockito: ^5.5.0
  mocktail: ^1.0.4 # Atau versi terbaru yang kompatibel
  build_runner: ^2.5.4 # Atau versi terbaru yang kompatibel
  analyzer: ^7.7.1 # Menambahkan analyzer secara eksplisit (gunakan versi terbaru yang kompatibel)
  bloc_test: ^9.1.7 # Untuk testing BLoC
  
  # Icon dan Splash Screen
  flutter_launcher_icons: ^0.13.1
  flutter_native_splash: ^2.3.13

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - assets/images/
    - assets/logo/
    - assets/icons/
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
