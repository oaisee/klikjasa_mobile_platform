/// FILE INI TIDAK DIGUNAKAN LAGI.
///
/// Semua proses Dependency Injection (DI) untuk fitur notifikasi telah dipusatkan
/// di `injection_container.dart` menggunakan `get_it` untuk konsistensi.
///
/// BLoC disediakan ke widget tree melalui `BlocProvider` di `app_router.dart`.
/// File ini dipertahankan sementara untuk menghindari error impor, tetapi akan dihapus di masa mendatang.
library;

class ProviderNotificationDI {}

